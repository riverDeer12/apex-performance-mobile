import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'software.rdd.apexPerformance',
  appName: 'Apex Performance',
  webDir: 'www'
};

export default config;
